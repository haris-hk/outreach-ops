# Outreach-Ops — Codex wrapper

Read and follow `AGENTS.md` (canonical agent instructions).
Slash commands are not guaranteed in Codex — run modes by name, e.g.
"Run the outreach-ops grade mode for https://example.com".
Headless workers: `codex exec "<prompt>"`.
