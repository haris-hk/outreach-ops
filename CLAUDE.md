# Outreach-Ops — Claude Code wrapper

@AGENTS.md

All agent instructions live in `AGENTS.md` (canonical, shared across CLIs).
Claude Code specifics: the `/outreach-ops` skill is registered from
`.claude/skills/outreach-ops/SKILL.md`; headless batch workers use
`claude -p` (see `batch/batch-runner.sh`).
